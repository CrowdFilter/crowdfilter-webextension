'use strict';

var comment_element_id_prefix = "posting_";
var comment_element_classes = null;
var injection_element_identifier = ".metabar";
var clicked_source = "heise";

injectButton(injection_element_identifier);

// Response that will be passed back by "executeScript"
var result = "Injection complete!";
result;
